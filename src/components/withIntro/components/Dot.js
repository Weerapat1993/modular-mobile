import React from 'react'
import { View } from 'react-native'
import styles from './styles'

export const Dot = () => (
  <View style={styles.dotStyle} />
)

export const DotActive = () => (
  <View style={styles.dotActiveStyle} />
)